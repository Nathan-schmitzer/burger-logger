<li>
    {{burger_name}}

    <button class="change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
        {{#if devoured}}DEVOURED!{{else}}Not eaten!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}" data-burgername="{{burger_name}}">Delete burger!</button>
</li>